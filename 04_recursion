#include <stdio.h>
int multiply(int n, int i){
    if(i==11){
        return 0;
    }
    printf("%d\n", n*i);
    return multiply(n, i+1);
}
int main() {
    int num;
    printf("Enter your number: ");
    scanf("%d", &num);
    multiply(num, 1);
    return 0;
}
